#include<iostream>
using namespace std;

int removeDuplicates(int arr[],int n)
{
    if(n==0)
        return 0;

    int index=1;

    for(int i=1;i<n;i++)
    {
        if(arr[i]!=arr[i-1])
            arr[index++]=arr[i];
    }

    return index;
}
